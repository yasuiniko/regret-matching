[project]
name = "regret-matching"
version = "0.1.0"
description = "Small visualizations for repeated games."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "matplotlib>=3.9.4",
    "numpy>=2.0.2",
    "scipy>=1.13.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.7",
]
